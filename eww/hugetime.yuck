(defpoll hugehours
	:interval "60s"
	:initial "initial-value"
	`date +%H`
)

(defpoll hugeminutes
	:interval "60s"
	:initial "initial-value"
	`date +%M`
)

(defpoll hugeseconds
	:interval "1s"
	:initial "initial-value"
	`date +%S`
)

(defwindow hugetime
  :monitor 0
  :geometry (geometry :anchor "top center")
  :stacking "bg"
  :exclusive false
	(box :class "bigtime" :orientation "h" :halign "start" :spacing 20 
		(hour-block) 
		(label :class "separator" :text ":")
		(minute-block)
		(label :class "separator" :text ":")
		(second-block)
	))


(defwidget hour-block []
  (box :class "timeblock hourblock"
       :orientation "v"
       :halign "center"
       :valign "center"
       (label :class "time-number"
              :text "${hugehours}")))

(defwidget minute-block []
  (box :class "timeblock minuteblock"
       :orientation "v"
       :halign "center"
       :valign "center"
       (label :class "time-number"
              :text "${hugeminutes}")))

(defwidget second-block []
  (box :class "timeblock secondblock"
       :orientation "v"
       :halign "center"
       :valign "center"
       (label :class "time-number"
              :text "${hugeseconds}")))


